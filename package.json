{
  "name": "color-app",
  "version": "1.0.0",
  "description": "A simple web app to change background color",
  "main": "app/server.js",
  "scripts": {
    "test": "mocha tests/**/*.test.js" // This runs all tests in the tests directory
  },
  "dependencies": {
    "express": "^4.17.1",
    "ejs": "^3.1.6"
  },
  "devDependencies": {
    "mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha-teamcity-reporter": "^1.0.0"
  }
}
