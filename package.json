{
  "name": "color-app",
  "version": "1.0.0",
  "description": "A simple web app that changes background color based on hex code",
  "main": "app/server.js",
  "scripts": {
    "start": "node app/server.js",
    "test": "mocha tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "ejs": "^3.1.6"
  },
  "devDependencies": {
    "mocha": "^9.1.3",
    "assert": "^2.0.0"
  }
}
